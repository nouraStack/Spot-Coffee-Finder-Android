# Security

This public portfolio repository intentionally excludes API keys, Firebase environment files, signing credentials, and local development configuration.

If you fork or clone the project, use your own restricted Google Maps key and your own Firebase project. Do not commit production credentials or Android signing keys.
